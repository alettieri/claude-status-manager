services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: status_manager
      POSTGRES_USER: sm
      POSTGRES_PASSWORD: sm_local
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
